namespace MartenIdentity.Aspire.Web.Resources;

/// <summary>
/// Marker type for the sample app's own UI strings — the generic argument of
/// <c>IStringLocalizer&lt;Strings&gt;</c>. Kept separate from the library's
/// <c>IdentityStrings</c>: the host app translates its own content independently
/// of the identity UI it consumes.
/// </summary>
public sealed class Strings;
