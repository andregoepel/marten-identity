@page "/Account/Lockout"
@attribute [AllowAnonymous]
@inherits IdentityLocalizedComponentBase

@inject NavigationManager NavigationManager

<AppPageTitle Title="@T("Lockout.PageTitle")" />

<div class="rz-p-4 rz-p-md-8">
    <RadzenCard>
        <RadzenStack Orientation="Orientation.Vertical" Gap="2rem" AlignItems="AlignItems.Center">

            <RadzenText TextStyle="TextStyle.H3" TagName="TagName.H1">@T("Lockout.Heading")</RadzenText>

            <RadzenAlert AlertStyle="AlertStyle.Danger" Shade="Shade.Lighter" Variant="Variant.Flat" AllowClose="false">
                @T("Lockout.Detail")
            </RadzenAlert>

            <RadzenButton Text="@T("Lockout.BackToLogin")"
                          ButtonStyle="ButtonStyle.Primary"
                          Click=@(() => NavigationManager.NavigateTo("/Account/Login")) />

        </RadzenStack>
    </RadzenCard>
</div>

@code {
}
