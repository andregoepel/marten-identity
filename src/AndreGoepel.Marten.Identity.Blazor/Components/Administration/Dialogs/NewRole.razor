@inject DialogService DialogService
@inject RoleManager<Role> RoleManager

<RadzenStack Gap="1rem" Orientation="Orientation.Vertical" JustifyContent="JustifyContent.SpaceBetween">
    <RadzenStack>
        <RadzenLabel Text="Rolename" Component="Rolename" />
        <RadzenTextBox Name="Rolename" Style="width: 100%" @bind-Value="@Rolename" />
    </RadzenStack>
    <RadzenStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.End" Gap="0.5rem">
        <RadzenButton Click="@(() => SaveRole())" ButtonStyle="ButtonStyle.Primary" Text="Save" Style="width: 120px" />
        <RadzenButton Click="@(() => DialogService.Close(false))" ButtonStyle="ButtonStyle.Light" Text="Close" Style="width: 120px" />
    </RadzenStack>
</RadzenStack>

@code {
    private string Rolename { get; set; } = "";

    private async Task SaveRole()
    {
        var newRole = new Role { Name = Rolename };

        var result = await RoleManager.CreateAsync(newRole);

        if (!result.Succeeded)
        {
            await DialogService.Alert($"Role cannot be added: {string.Join("\r\n", result.Errors.Select(error => error.Description))}", "Error adding role");
        }
        else
        {
            DialogService.Close(true);
        }
    }
}
