name: E2E

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  e2e:
    uses: andregoepel/workflows/.github/workflows/e2e.yml@main
    with:
      e2e-project: tests/AndreGoepel.Marten.Identity.E2ETests/AndreGoepel.Marten.Identity.E2ETests.csproj
